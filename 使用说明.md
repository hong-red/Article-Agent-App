# 妙文 · 使用说明文档

> 面向个人自媒体与公众号运营者的智能写作助手
> 版本：网页版 v1.1（APP 安卓版同步更新）

---

## 一、产品简介

**妙文**（取自“铁肩担道义，妙手著文章”）是一款智能写作辅助工具，帮助用户完成从**选题、成文、选图、排版到推送微信公众号草稿箱**的全流程操作。

- **网页版**：浏览器直接访问，无需安装。
- **APP 版**：安卓手机安装使用（支持预览）。
- **适用人群**：个人自媒体、学校/单位公众号运营者，包括不熟悉大模型工具的中老年用户。

---

## 二、网页版使用说明

### 1. 访问与登录

| 步骤 | 操作 |
|------|------|
| 1 | 浏览器打开 `http://81.70.191.44:8000` |
| 2 | 右上角「设置」→「访问口令」，填写 `AMFATCsyrszD` |
| 3 | 点击「连接并加载配置」，系统自动拉取已配置密钥 |

完成后即可正常进行选题、成文、推送草稿等操作。

### 2. 核心流程（四步）

```
选题 → 成文 → 选图 → 排版 → 推送到草稿箱
```

#### 第一步：选题
- 系统自动生成选题。
- 如对选题不满意，可在**第二步手动更改**。
- 建议：官媒类公众号可上传会议资料等文字素材，让大模型根据素材生成文章（素材库支持，见「本地库说明」）。

#### 第二步：成文
- 提供两种方式：
  - **修改建议**：向 AI 提出要求（如字数、语气、结构），先生成一版。
  - **自主编译**：在生成结果基础上直接修改正文。
- 字数控制：在修改意见中说明字数要求，或通过自主编译调整。
- AI 生成内容不一定完全满足要求，**需人工审阅修改**。

#### 第三步：选图 / 上传图片
- 可上传本地图片，系统自动插入。
- 若插入位置不对，可手动调整对应代码。
- 复制粘贴图片需在此步完成。

#### 第四步：排版与推送
- 样式可调整（整个文件均可编辑）。
- 确认无误后，一键推送到微信公众号草稿箱。

### 3. 本地库说明

本地库包含两类内容（网页版与 APP 版一致）：

| 类型 | 说明 |
|------|------|
| **文章库** | 后期生成的文章，不一定马上上传，可先保存。点击文件即可载入显示、继续修改，确认后推送到微信草稿箱。**文章存储在服务器上。** |
| **素材库** | 自己上传的素材文件，**不仅限于图片**，也可上传文字素材（如会议纪要、讲话稿、PDF 等），供 AI 参考生成文章。 |

### 4. 图片操作详解

| 场景 | 操作说明 |
|------|----------|
| 插入位置 | 图片可插入正文，但 AI 识别不一定精准，有时会放到文章最后 |
| 移动图片 | 图片在 Markdown 中以「文件名 + 路径后缀」形式存在，找到对应代码轻轻粘贴即可挪到合适位置 |
| 删除图片 | 在**左侧编辑区**操作（右侧预览区点击无效） |
| 复制粘贴图片 | 需在**第三步**上传本地图片，之后自动插入；位置不对时调整代码 |
| 网络图片 | 网上下载的图片同样有代码，可手动调整位置 |
| 封面图 | **只能上传**，不能使用网络图片；不上传则使用默认封面 |

### 5. 推送草稿箱

- 推送微信公众号草稿箱功能可用。
- **默认配置**：推送到开发者自己的草稿箱。
- **用户使用**：需手动设置自己的公众号 AppID/Secret（详见「五、微信公众号设置」）。
- 微信公众号推送**无字数限制，仅对摘要有限制**；标题和摘要字长限制可在界面小框中调节。

### 6. 刷新说明（网页版）

| 需求 | 操作 |
|------|------|
| 想退回第一步选题重新开始 | **刷新浏览器页面**（按 F5，或手机浏览器下拉刷新）即可 |
| 页面更新后仍是旧界面 | 网页版是 PWA，会缓存旧文件；请**强制刷新**（电脑 `Ctrl + F5`，手机清浏览器缓存或关掉标签页重开） |
| 手机已「添加到主屏」的入口 | 版本更新后若仍打开旧页面，需删除主屏图标重新添加，或在浏览器中清缓存 |

---

## 三、APP 版使用说明（安卓）

### 1. 安装与注册

| 步骤 | 操作 |
|------|------|
| 1 | 将 APK 传到手机 → 安装（允许「未知来源」） |
| 2 | 打开「妙文」→ 切到「注册」→ 填**用户名（2~20 字）+ 密码（至少 6 位）+ 邀请码** |
| 3 | 注册成功后用该账号登录 |
| 4 | 右上角「设置」填写 **DeepSeek Key**（必填）+ 公众号 **AppID/Secret**（推送草稿箱才需要） |
| 5 | 四步流程：选题 → 成文 → 选图 → 排版 → 推送到草稿箱 |

### 2. 邀请码与注册名额（重要）

- 邀请码：`SMART2026`
- 该邀请码**共可注册 200 个账号**（先到先得）。
- 名额用完后会提示「邀请码无效或已被使用」，此时需联系管理员获取新邀请码。
- 每个账号的用户名唯一，请勿重复注册。

### 3. 测试账号

- 用户名：`ceshi888`
- 密码：`ceshi123456`

### 4. 编译 / 预览模式开关（重要）

由于手机屏幕较小，直接编辑 Markdown 源码不方便查看最终效果，APP 在**第二步「正文」**和**第四步「结果」**区域提供了**「编辑 / 预览」开关**：

| 模式 | 显示内容 | 用途 |
|------|----------|------|
| **编辑模式**（默认） | Markdown 源码 | 直接修改正文、调整图片代码位置 |
| **预览模式** | 排版后的阅读效果 | 小屏手机上查看文章最终样子、检查图片位置 |

- 点开关即可在两种模式间切换，切换不会丢失已编辑的内容。
- 网页版因屏幕较大，编辑区与预览区同时显示，无需此开关。

### 5. 本地库（文章库 + 素材库）

APP 版本地库与网页版一致，分两个标签页：

- **文章库**：保存 / 载入 / 删除已生成的文章，可下拉刷新。
- **素材库**：点击「上传素材」可从手机选择**任意文件**（文字、图片、文档等）上传，供 AI 参考；支持删除。上传后的素材同样存储在服务器上。

### 6. 刷新与重启（APP 版）

| 需求 | 操作 |
|------|------|
| 本地库列表刷新 | 在列表上**下拉刷新** |
| 想重新开始选题流程 | 退出登录后重新登录，或**关闭 App 重开**（App 当前无「回到第一步」按钮） |
| 设置保存后未生效 | 重新进入对应步骤，或重启 App |

### 7. 平台限制

- 目前仅支持**安卓**，苹果手机暂不支持。
- APP 支持预览，电脑上可模拟，但环境配置较复杂。

---

## 四、DeepSeek API 获取与配置（详细）

DeepSeek 是「妙文」用来生成选题、正文、排版的 AI 大模型，**必须配置 API Key 才能使用**。

### 1. 注册并登录

1. 打开 DeepSeek 开放平台官网：**https://platform.deepseek.com**
2. 使用手机号或邮箱注册账号，完成登录。
3. 注意：这是**开放平台**（API 接口），与网页版对话的 chat.deepseek.com 是两套独立的计费体系，**需要单独充值**。

### 2. 创建 API Key

1. 登录后，在左侧菜单找到 **「API Keys」**。
2. 点击 **「创建 API Key」**，为它起个名字（如「妙文」）。
3. 创建成功后，页面会显示一串以 **`sk-`** 开头的密钥。
4. ⚠️ **该密钥只完整显示这一次**，请立刻复制保存到安全的地方。若遗失，只能重新创建一个新 Key。

### 3. 充值（余额）

- API 调用需要账户内有余额，否则会报错。
- 在平台的 **「充值」/「Billing」** 页面充值。
- DeepSeek 按 token 用量计费，价格较低，具体价格以官网实时显示为准。
- 建议首次先小额充值（如 10~20 元）测试即可。

### 4. 在「妙文」中填写

| 位置 | 填写项 | 填什么 |
|------|--------|--------|
| 网页版 | 设置 → DeepSeek → API Key | 粘贴刚才的 `sk-...` |
| 网页版 | 设置 → DeepSeek → 模型 | `deepseek-chat`（通用、快，推荐写作）或 `deepseek-reasoner`（推理强、慢） |
| APP 版 | 设置 → DeepSeek（内容生成）→ API Key | 粘贴 `sk-...` |
| APP 版 | 设置 → 模型 | 默认 `deepseek-chat`，一般无需修改 |

### 5. 模型选择建议

| 模型 | 特点 | 适用场景 |
|------|------|----------|
| `deepseek-chat` | 快、便宜、通用 | 日常选题、成文、排版（推荐） |
| `deepseek-reasoner` | 推理更强、更慢 | 需要深度分析、复杂论证的文章 |

### 6. 官方文档

- API 文档：**https://api-docs.deepseek.com**
- 接口 Base URL（一般无需改）：`https://api.deepseek.com`

---

## 五、微信公众号设置（详细）

推送「草稿箱」需要对接微信公众平台接口。**只有配置了 AppID / AppSecret 才能推送；不推送则可不配置。**

### 1. 前提：需要一个「认证公众号」

- 登录微信公众平台：**https://mp.weixin.qq.com**
- 草稿箱推送接口要求公众号为**已认证的服务号**（错误码 `48001` 即表示未认证或无权限）。
- **个人订阅号**无法使用草稿箱接口，需升级为企业/组织认证的服务号。

### 2. 获取 AppID 与 AppSecret

1. 登录公众平台后，进入 **「设置与开发」→「基本配置」**。
2. 在 **「公众号开发信息」** 区域可看到：
   - **AppID**：公开可见，直接复制。
   - **AppSecret**：点击 **「重置」** 生成。
3. ⚠️ **AppSecret 只完整显示一次**，生成后请立即复制保存。遗失需重新重置（重置后旧 Secret 立即失效）。

### 3. 配置 IP 白名单（最常见的报错点）

微信公众号接口要求调用方（即**妙文所在的服务器**）的公网 IP 加入白名单，否则报错 `40164`。

| 步骤 | 操作 |
|------|------|
| 1 | 进入 **「设置与开发」→「基本配置」→「IP 白名单」** |
| 2 | 点击 **「查看 / 修改」** |
| 3 | 填入**后端服务器的公网出口 IP**（注意：是**服务器**的 IP，**不是你自己手机/电脑的 IP**） |
| 4 | 保存 |

- 后端提供了一个探测接口，可在浏览器打开 `http://81.70.191.44:8001/api/wechat/ip`（APP 后端）查看服务器公网 IP；网页版可在设置界面查看。
- 若服务器有多条出口线路，可能有多个 IP，需全部加入。

### 4. 在「妙文」中填写

| 位置 | 填写项 | 填什么 |
|------|--------|--------|
| 网页版 | 设置 → 微信公众号 → AppID | 复制的 AppID（`wx...`） |
| 网页版 | 设置 → 微信公众号 → AppSecret | 复制的 AppSecret |
| 网页版 | 设置 → 作者名 / 原文链接 | 可选 |
| APP 版 | 设置 → 微信公众号（可选）→ AppID / AppSecret | 同上 |

### 5. 推送后去哪里查看

- 推送成功后，文章进入该公众号的 **「草稿箱」**。
- 登录公众平台 → **「草稿箱」**，即可看到文章，可继续编辑后**群发**或定时发布。

### 6. 常见错误码对照

| 错误码 | 含义 | 解决办法 |
|--------|------|----------|
| `40164` | 服务器 IP 不在白名单 | 把**服务器公网 IP** 加入「IP 白名单」 |
| `40013` | AppID 无效 | 检查填写的 AppID |
| `40125` / `40001` / `41001` | AppSecret 错误或失效 | 重新核对/重置 AppSecret |
| `48001` | 公众号未认证或无权限 | 需使用已认证的服务号 |

---

## 六、常见问题

| 问题 | 解决方法 |
|------|----------|
| 如何退回第一步选题？ | **刷新页面**（网页版）；APP 版可退出重登或重启 App |
| 格式优化后出现异常？ | 多为 AI 生成时 HTML 标签未正确转换，需人工核对正文 |
| AI 生成内容不满足要求？ | 属正常情况，需人工审阅修改 |
| 字数如何控制？ | 在“修改建议”中提出字数要求，或通过“自主编译”再次修改 |
| 图片删不掉？ | 在**左侧编辑区**操作，右侧预览区无效 |
| 图片只能放最后？ | 可手动调整 Markdown 中的图片代码位置 |
| 注册提示邀请码无效？ | 邀请码可能已被用完（SMART2026 仅 200 个名额），联系管理员 |
| 生成正文报错？ | 检查 DeepSeek Key 是否填写正确、账户是否有余额 |
| 推送报 `40164`？ | 把服务器公网 IP 加入公众号「IP 白名单」 |

---

## 七、新手提示

- 未接触过大模型的用户，可能不了解其能力，建议在界面中多给出操作提示。
- 可用的提示语示例：
  - 开头更吸引人一点
  - 语气再活泼些
  - 加一个小结
  - 字数修改（如“控制在 1200 字左右”）
- 中老年用户群体较多，操作提示应更友好、直观。

---

## 八、后续计划

- 继续完善 APP 及视频教程。
- 补充详细使用说明（本文档持续更新）。
- 考虑在选题环节增加**字数限制选项**。
- 板块大体已确定，后续可能微调。

---

## 九、问题反馈与联系方式

使用过程中如遇到问题，或有功能建议，欢迎联系：

**邮箱：1850425474@qq.com**

---

## 十、更新记录

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0 | — | 网页版上线，支持选题、成文、选图、排版、推送草稿箱；APP 安卓版开发中 |
| v1.1 | 2026-09 | 名称由「智能精灵」统一改为「妙文」；补充 DeepSeek API 与微信公众号配置文档；新增刷新说明；APP 邀请码 SMART2026 支持 200 个账号；APP 本地库支持上传素材；APP 正文/结果新增「编辑 / 预览」模式开关 |

---

> 如后续有新增功能或调整，建议同步更新本文档，并在视频教程中重点演示**图片位置调整、本地库使用、字数修改、编辑/预览切换**等易错环节。
